diff --git a/node_modules/@convex-dev/auth/dist/server/implementation/tokens.js b/node_modules/@convex-dev/auth/dist/server/implementation/tokens.js
index fadbc0c..e18001b 100644
--- a/node_modules/@convex-dev/auth/dist/server/implementation/tokens.js
+++ b/node_modules/@convex-dev/auth/dist/server/implementation/tokens.js
@@ -1,10 +1,16 @@
-import { SignJWT, importPKCS8 } from "jose";
+import { SignJWT, calculateJwkThumbprint, importPKCS8 } from "jose";
 import { requireEnv } from "../utils.js";
 import { TOKEN_SUB_CLAIM_DIVIDER } from "./utils.js";
 const DEFAULT_JWT_DURATION_MS = 1000 * 60 * 60; // 1 hour
 const RESERVED_CLAIMS = new Set(["sub", "iss", "aud", "iat", "exp", "nbf", "jti"]);
 export async function generateToken(ctx, args, config) {
     const privateKey = await importPKCS8(requireEnv("JWT_PRIVATE_KEY"), "RS256");
+    const jwks = JSON.parse(requireEnv("JWKS"));
+    const signingJwk = jwks.keys?.[0];
+    if (!signingJwk) {
+        throw new Error("JWKS must contain at least one signing key");
+    }
+    const kid = signingJwk.kid ?? await calculateJwkThumbprint(signingJwk);
     const expirationTime = new Date(Date.now() + (config.jwt?.durationMs ?? DEFAULT_JWT_DURATION_MS));
     const extraClaims = {};
     if (config.jwt?.customClaims) {
@@ -23,7 +29,7 @@ export async function generateToken(ctx, args, config) {
         ...extraClaims,
         sub: args.userId + TOKEN_SUB_CLAIM_DIVIDER + args.sessionId,
     })
-        .setProtectedHeader({ alg: "RS256" })
+        .setProtectedHeader({ alg: "RS256", kid, typ: "JWT" })
         .setIssuedAt()
         .setIssuer(requireEnv("CONVEX_SITE_URL"))
         .setAudience("convex")
diff --git a/node_modules/@convex-dev/auth/src/server/implementation/tokens.ts b/node_modules/@convex-dev/auth/src/server/implementation/tokens.ts
index dba91b3..9867491 100644
--- a/node_modules/@convex-dev/auth/src/server/implementation/tokens.ts
+++ b/node_modules/@convex-dev/auth/src/server/implementation/tokens.ts
@@ -1,6 +1,6 @@
 import { GenericId } from "convex/values";
 import { ConvexAuthConfig } from "../index.js";
-import { SignJWT, importPKCS8 } from "jose";
+import { SignJWT, calculateJwkThumbprint, importPKCS8 } from "jose";
 import { requireEnv } from "../utils.js";
 import { TOKEN_SUB_CLAIM_DIVIDER } from "./utils.js";
 import { MutationCtx } from "./types.js";
@@ -18,6 +18,17 @@ export async function generateToken(
   config: ConvexAuthConfig,
 ) {
   const privateKey = await importPKCS8(requireEnv("JWT_PRIVATE_KEY"), "RS256");
+  const jwks = JSON.parse(requireEnv("JWKS")) as {
+    keys?: Array<Record<string, unknown> & { kid?: string }>;
+  };
+  const signingJwk = jwks.keys?.[0];
+
+  if (!signingJwk) {
+    throw new Error("JWKS must contain at least one signing key");
+  }
+
+  const kid =
+    signingJwk.kid ?? (await calculateJwkThumbprint(signingJwk));
   const expirationTime = new Date(
     Date.now() + (config.jwt?.durationMs ?? DEFAULT_JWT_DURATION_MS),
   );
@@ -40,7 +51,7 @@ export async function generateToken(
     ...extraClaims,
     sub: args.userId + TOKEN_SUB_CLAIM_DIVIDER + args.sessionId,
   })
-    .setProtectedHeader({ alg: "RS256" })
+    .setProtectedHeader({ alg: "RS256", kid, typ: "JWT" })
     .setIssuedAt()
     .setIssuer(requireEnv("CONVEX_SITE_URL"))
     .setAudience("convex")
